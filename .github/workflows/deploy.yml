name: Build and Deploy Curriculum Service

on:
  push:
    branches:
      - main
  workflow_dispatch:

env:
  IMAGE_NAME: zerde-curriculum-service
  COMPOSE_SERVICE: curriculum-service

concurrency:
  group: zerde-production-curriculum-service-deploy
  cancel-in-progress: true

jobs:
  build-and-push:
    name: Build and push Curriculum Service
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Login to Docker Hub
        uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v7
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME }}/${{ env.IMAGE_NAME }}:${{ github.sha }}
            ${{ secrets.DOCKERHUB_USERNAME }}/${{ env.IMAGE_NAME }}:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

  deploy:
    name: Deploy Curriculum Service to server
    runs-on: ubuntu-latest
    needs: build-and-push

    steps:
      - name: Deploy service on server
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          password: ${{ secrets.SERVER_PASSWORD }}
          port: ${{ secrets.SERVER_SSH_PORT }}
          script: |
            set -e
            cd "${{ secrets.SERVER_DEPLOY_PATH }}"

            if [ ! -f docker-compose.prod.yml ]; then
              echo "docker-compose.prod.yml not found in ${{ secrets.SERVER_DEPLOY_PATH }}. Run infra deploy first."
              exit 1
            fi

            echo '${{ secrets.DOCKERHUB_TOKEN }}' | docker login -u '${{ secrets.DOCKERHUB_USERNAME }}' --password-stdin

            docker compose -f docker-compose.prod.yml pull "${{ env.COMPOSE_SERVICE }}"
            docker compose -f docker-compose.prod.yml up -d --no-deps "${{ env.COMPOSE_SERVICE }}"
            docker image prune -f